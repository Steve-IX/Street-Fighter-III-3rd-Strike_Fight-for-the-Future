{
  "name": "arcade-link-local",
  "version": "0.1.0",
  "private": true,
  "description": "Local-ROM FBNeo web launcher with input mapping and private room signaling.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js",
    "test": "node --test test/server.test.js",
    "verify-assets": "node scripts/verify-emulator-assets.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}